/**
 * Z 字形变换
 * 寻找数规律
 * @param s
 * @param numRows
 */
function convert(s: string, numRows: number): string {
  return ''
}

import { testFunction } from '../../utils'
const testCases = [['PAYPALISHIRING', 3]]
const expectedResults = ['PAHNAPLSIIGYIR']
testFunction(convert, testCases, expectedResults)
